rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // User profiles
    match /users/{userId} {
      allow read, write: if isOwner(userId);
      
      // Sub-collections
      match /diet_records/{recordId} {
        allow read, write: if isOwner(userId);
      }
      
      match /custom_foods/{foodId} {
        allow read, write: if isOwner(userId);
      }
      
      match /weight_records/{recordId} {
        allow read, write: if isOwner(userId);
      }
      
      match /water_records/{recordId} {
        allow read, write: if isOwner(userId);
      }
      
      match /workout_records/{recordId} {
        allow read, write: if isOwner(userId);
      }
      
      match /settings/{docId} {
        allow read, write: if isOwner(userId);
      }
    }

    // Global online foods database (read/write for all users without author tagging)
    match /cloud_foods/{foodId} {
      allow read: if true;
      allow create, update: if isAuthenticated();
    }
  }
}
